ZPRD configuration file [layout ...]

LOCATION:
  /etc/zrpd.conf

LINE FORMAT:
  CMD ARGS...

CMDs:
  #  comment
  A  ip address
  I  interface
  L  local route address
  R  remote
  T  remote timeout (re-resolve remote)
  U  drop privs to. user

EXAMPLE:
  @ see doc/files/zprd.conf
